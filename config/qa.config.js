// config/qa.config.js
// QA/staging environment configuration for ReqRes API
// (Same URL as dev here, but split for real-world flexibility.)

module.exports = {
  envName: 'qa',
  baseURL: 'https://reqres.in/api',
};

